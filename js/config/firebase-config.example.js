// ABOUTME: Example Firebase configuration template for Track Manager PWA
// ABOUTME: Copy this file to firebase-config.js and replace placeholder values with actual credentials

/**
 * Firebase Configuration Example
 *
 * To set up Firebase configuration:
 * 1. Copy this file to firebase-config.js in the same directory
 * 2. Go to Firebase Console (https://console.firebase.google.com/)
 * 3. Select your project
 * 4. Go to Project Settings (gear icon)
 * 5. Scroll down to "Your apps" section
 * 6. Select your web app or click "Add app" to create one
 * 7. Copy the configuration values and replace the placeholders below
 *
 * Note: firebase-config.js is gitignored to keep your credentials private
 */

export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
  appId: "YOUR_APP_ID"
};
